TASK: Add mobile tests, GitHub quality checks, secret scanning, and Android release validation.

REPOSITORY

Work only in:

shriqrdotcom/App-Manager-07

Expo application directory:

frontend

PREREQUISITE

Prompt 5 must already be reviewed and merged into "origin/main".

Before changing anything:

1. Fetch origin.
2. Confirm the repository is "shriqrdotcom/App-Manager-07".
3. Confirm the working tree is clean.
4. Switch to "main".
5. Update it from "origin/main" using fast-forward only.
6. Confirm Prompt 5 changes are present.
7. Run the existing frozen dependency installation, TypeScript, lint, Expo dependency, and Expo Doctor checks.
8. Stop if any prerequisite fails.
9. Confirm this branch does not already exist locally or remotely:

test/mobile-release-gates

10. Create:

test/mobile-release-gates

GOAL

Create a reliable automated quality gate for the Expo Android application.

Add:

- meaningful mobile unit and component tests
- deterministic CI test commands
- GitHub Actions checks
- full-history secret scanning
- production configuration validation
- Android JavaScript export validation
- a documented release-validation command

Do not build or submit a production AAB in this task.

Do not modify the backend repository.

STRICT IDENTITY PROTECTION

Read and record the confirmed values for:

- Expo owner
- Expo slug
- EAS project ID
- Android package
- app scheme
- app version
- EAS version source

Do not change any of these values.

Do not:

- create a new EAS project
- change the Android package
- reset Android version codes
- change the Expo SDK
- add Expo Updates
- change signing credentials
- change app functionality merely to make tests pass
- weaken TypeScript, lint, Expo Doctor, or release checks

If the identity differs from the Prompt 5 baseline, stop and return "BLOCKED FOR REVIEW".

1. INSPECT EXISTING TEST INFRASTRUCTURE

Before adding packages, inspect:

- existing test files
- existing Jest configuration
- "frontend/package.json"
- TypeScript configuration
- Expo Router configuration
- path aliases
- existing mocks
- existing CI workflows
- existing security-scanning configuration

Reuse valid existing infrastructure.

Do not create a second competing test framework.

If no suitable mobile test framework exists, add an Expo SDK-compatible setup using:

- Jest
- "jest-expo"
- React Native Testing Library
- required Jest TypeScript types
- a React renderer version compatible with the current React version, only if required

Install only SDK-compatible versions.

Use Yarn Classic and the public lockfile.

Do not use:

- "npm install"
- "--force"
- "--legacy-peer-deps"
- private Replit registries
- unpinned incompatible package versions

2. TEST CONFIGURATION

Add deterministic test commands without removing valid existing scripts.

Required scripts should include equivalent commands for:

- normal test execution
- CI test execution
- test coverage
- release-configuration validation
- complete local release validation

Suggested script names:

- "yarn test"
- "yarn test:ci"
- "yarn test:coverage"
- "yarn validate:release-config"
- "yarn validate:release"

Use the repository’s existing naming conventions if equivalent scripts already exist.

Requirements:

- CI tests must be non-interactive
- tests must terminate cleanly
- tests must not depend on execution order
- tests must not make real network requests
- tests must not contact Google OAuth
- tests must not contact the production backend
- tests must not require EAS credentials
- reset mocks and timers between tests
- do not hide console errors that indicate real failures
- do not use large snapshots as a substitute for behavioural assertions

Run a real coverage command and report the measured result.

Do not invent a coverage percentage.

If no coverage threshold currently exists, do not introduce an arbitrary global percentage simply to claim production readiness. Establish genuine passing tests and report the measured baseline.

3. MINIMUM MOBILE TEST COVERAGE

Inspect the actual implementation and add focused tests for existing behaviour.

Do not invent features or APIs that the application does not contain.

At minimum, cover these areas where corresponding code exists:

Authentication and session behaviour:

- signed-out state routes to sign-in
- valid restored session does not show sign-in
- expired or rejected session is handled safely
- API "401" triggers the existing invalid-session behaviour
- no bearer token is invented by the mobile app
- authentication secrets are not stored in ordinary client state

Restaurant context:

- selected restaurant comes from the validated bootstrap response
- invalid or missing restaurant selection is handled safely
- a submitted or manually constructed restaurant ID is not silently trusted
- restaurant switching clears restaurant-specific stale state where implemented

Role behaviour:

- Owner receives the implemented restaurant features
- Admin receives the implemented restaurant features
- Manager receives the implemented restaurant features
- Staff is excluded from restricted menu-management UI
- unknown or malformed roles fail closed
- Superadmin is not treated as a normal mobile restaurant role

API behaviour:

- authenticated requests use the existing authenticated API client
- malformed JSON is handled safely
- "401", "403", network failure, and server failure are distinguished
- retry behaviour does not duplicate mutation requests
- runtime response validation rejects malformed data
- tests use mocked fetch responses, never live services

Navigation and critical screens:

- sign-in screen renders
- authenticated home renders
- Settings/App Information renders
- the displayed app version is not a hardcoded value
- critical navigation routes do not crash
- restricted navigation items follow actual role rules

Production and demo behaviour:

- demo mode is disabled when production validation runs
- production cannot silently use mock data
- missing required production configuration fails clearly
- preview-only flags do not enable themselves from arbitrary truthy strings

Add tests only for behaviour that can be verified from the repository.

If testing requires a small extraction of pure logic, keep it narrowly scoped and preserve runtime behaviour exactly.

Do not redesign screens or change business behaviour during this task.

4. RELEASE-CONFIGURATION VALIDATOR

Create a small deterministic Node-based validation script inside "frontend/scripts" or the existing scripts directory.

It must validate the current confirmed production baseline without accessing secrets.

Validate:

- Expo owner is unchanged
- slug is unchanged
- EAS project ID is unchanged
- Android package is unchanged
- application scheme is unchanged
- app version is valid and present
- EAS version source remains remote
- production profile uses the production environment
- production Android output is an app bundle
- production is not a development client
- production does not use internal distribution
- Android auto-increment remains enabled
- adaptive icon exists
- adaptive icon is a valid PNG
- adaptive icon is exactly "1024 × 1024"
- production demo flag is not enabled
- required public backend configuration is present and uses HTTPS
- no secret values are embedded in "app.json" or "eas.json"

The confirmed identity values are non-secret and may be used as explicit release guardrails. Do not guess them—read them from the verified Prompt 5 baseline.

The image validator should read the PNG header directly or use an already-installed safe dependency. Do not add a heavy runtime image package only to check dimensions.

The validation script must:

- return exit code "0" on success
- return a non-zero exit code on failure
- provide safe and understandable error messages
- never print environment-variable values
- never print credentials

5. GITHUB ACTIONS QUALITY GATE

Create a workflow at an appropriate root path such as:

.github/workflows/mobile-quality-gates.yml

Trigger it on:

- pull requests targeting "main"
- pushes to "main"
- manual "workflow_dispatch"

Limit path filters to the mobile application, lockfile, and relevant workflow/configuration files where appropriate.

Use stable check names:

- Mobile tests
- Expo validation
- Secret scan
- Release validation

Security requirements:

- set minimum workflow permissions, normally "contents: read"
- do not use "pull_request_target"
- do not expose repository secrets to untrusted pull-request code
- do not use writable GitHub tokens unnecessarily
- set reasonable job timeouts
- cancel outdated runs for the same pull request
- use Node 20 as established by the repository
- install with "yarn install --frozen-lockfile --non-interactive"
- use the existing public Yarn lockfile
- disable persistent checkout credentials when they are unnecessary
- never execute untrusted pull-request text in a shell
- do not upload ".env" files or environment dumps
- do not print GitHub context objects containing tokens

Pin every third-party GitHub Action to a verified immutable full commit SHA.

Add a comment beside each SHA identifying the corresponding reviewed release tag.

Do not use floating references such as:

- "@main"
- "@master"
- "@v4" without an immutable SHA

Use only official or well-maintained actions.

If an action’s immutable commit cannot be verified, stop and report it rather than guessing a SHA.

6. MOBILE TESTS CHECK

The "Mobile tests" check must run from "frontend" and include:

- frozen Yarn installation
- TypeScript checking
- lint
- unit/component tests
- coverage generation

Coverage output must not contain credentials or environment values.

Do not commit the generated coverage directory.

7. EXPO VALIDATION CHECK

The "Expo validation" check must include:

- Expo dependency compatibility check
- complete Expo Doctor
- resolved public Expo configuration validation
- parsing of "app.json" and "eas.json"
- adaptive-icon validation
- confirmation that the EAS identity remains unchanged

Do not suppress Expo Doctor checks.

Do not require an EAS token for ordinary pull-request validation.

8. SECRET SCANNING CHECK

Add a maintained secret scanner such as Gitleaks, provided its official release and immutable action SHA can be verified.

Requirements:

- scan the complete Git history
- use checkout history depth "0" for this job
- scan the current worktree
- redact detected secret values from logs
- fail when a credible secret is detected
- do not print the matched secret
- do not upload the secret as an artifact
- do not use broad allowlists
- do not ignore entire source directories

False positives may be allowlisted only when all of the following are true:

- the match was manually inspected
- it is proven not to be a real credential
- the exception is narrow and exact
- the reason is documented
- the exception cannot hide future credentials

If a real or suspected credential is found:

1. Do not reveal its value.
2. Do not rewrite Git history.
3. Do not rotate it automatically.
4. Report only the file path, commit identifier, credential type, and whether it exists in the current tree or history.
5. Stop and return "BLOCKED: SECRET REMEDIATION REQUIRED".

Also inspect whether GitHub’s native:

- secret scanning
- push protection

are enabled using read-only repository information, if accessible.

Do not change repository security settings. If their status cannot be verified, report the manual setting the repository owner must enable.

9. RELEASE VALIDATION CHECK

The "Release validation" check must simulate safe Android release preparation without building or submitting an AAB.

It must run:

- release-configuration validator
- production-style Expo public config resolution
- production-style Android Expo export
- demo mode explicitly disabled
- required public backend URL supplied through a GitHub repository variable or another non-secret approved mechanism

Use this GitHub repository variable name:

EXPO_PUBLIC_BACKEND_URL

Do not place the backend URL in GitHub Secrets unless repository policy requires it; it is a public mobile configuration value, not a backend credential.

Never place backend secrets in "EXPO_PUBLIC_*".

If the GitHub repository variable is missing, make the workflow fail with a safe message containing only the missing variable name.

The Android export must:

- use a temporary output directory
- not trigger an EAS cloud build
- not access signing credentials
- not submit to Google Play
- not upload the export as a workflow artifact
- remove generated output after validation

This check validates JavaScript bundling and configuration. It does not claim that a signed production AAB was successfully built.

10. RELEASE DOCUMENTATION

Add a short mobile release-check document or update the existing relevant documentation.

Document:

- local validation command
- GitHub check names
- required Node and Yarn versions
- required GitHub repository variable name
- confirmation that the variable is public configuration, not a secret
- what each quality check validates
- what the checks do not validate
- that a real EAS production build and Play internal test happen in a later task
- that all four checks should be required before merging release-related changes

Do not include credentials, tokens, environment values, or private URLs.

11. LOCAL VALIDATION

Run from a clean installation.

At minimum run:

- "yarn install --frozen-lockfile --non-interactive"
- TypeScript typecheck
- lint
- complete test suite
- real coverage command
- Expo dependency check
- complete Expo Doctor
- release-configuration validator
- production-style Android Expo export
- "git diff --check"

Also validate:

- workflow YAML syntax
- no GitHub Actions use floating tags
- job permissions are least-privilege
- no workflow uses "pull_request_target"
- no generated test coverage or export output is tracked
- no private registry URLs appear in the lockfile
- no secrets are present in changed files
- no application identifiers changed
- no production functionality was changed to satisfy a test

Perform one clean-room verification using the committed lockfile and an empty dependency cache when practical.

12. PROHIBITED ACTIONS

Do not:

- modify "shriqrdotcom/exzibo-mag"
- call production APIs
- use real Google authentication in tests
- send orders, bookings, messages, or notifications
- modify application features or UI
- change EAS identity
- change Android package
- change app version
- reset version codes
- access or replace signing credentials
- trigger an EAS build
- create an APK or AAB
- submit to Google Play
- create a GitHub release
- modify branch protection
- merge into "main"
- rewrite Git history
- rotate or reveal secrets
- commit ".env" files
- commit test coverage
- commit generated Expo output
- commit prompts or "attached_assets"

13. GIT

Review the complete diff before committing.

Expected changes may include:

- test files
- Jest configuration
- test setup and mocks
- release-validation script
- ".github/workflows/mobile-quality-gates.yml"
- mobile release-check documentation
- "frontend/package.json"
- "frontend/yarn.lock"
- ".gitignore", only for generated coverage/export exclusions

Commit only when local validation passes and no real secret is detected.

Commit message:

test: add mobile release quality gates

Push only to:

origin/test/mobile-release-gates

Do not create or merge a pull request.

FINAL RESPONSE

Return:

1. Repository, branch, and starting commit
2. Prompt 5 prerequisite results
3. Confirmed EAS identity before and after
4. Existing test infrastructure found
5. Test framework and dependencies added
6. Tests added, grouped by feature
7. Exact test results
8. Real measured coverage results
9. Release-validator rules
10. GitHub workflow and trigger summary
11. Exact GitHub status-check names
12. Secret-scanner implementation
13. Secret-scanning result without secret values
14. GitHub native secret-scanning and push-protection status
15. GitHub repository variables required
16. Expo Doctor, typecheck, lint, dependency, and export results
17. Files changed
18. Lockfile and registry verification
19. Commit hash and push result
20. Manual steps required before merging
21. Remaining blockers for Prompt 7
22. Final result: "PASS", "FAIL", "BLOCKED FOR REVIEW", or "BLOCKED: SECRET REMEDIATION REQUIRED"

PASS CONDITIONS

Return "PASS" only when:

- frozen dependency installation succeeds
- TypeScript passes
- lint passes
- all tests pass
- real coverage was measured and reported
- Expo dependency validation passes
- complete Expo Doctor passes
- release configuration passes
- production demo mode is disabled
- Android production-style export succeeds
- adaptive icon remains valid
- EAS identity remains unchanged
- GitHub workflow syntax is valid
- Actions are pinned to verified immutable SHAs
- secret scanning is active in the workflow
- no credible secret is detected
- lockfile contains no private registry URLs
- no generated output is tracked
- the branch is committed and pushed successfully

A local "PASS" does not mean the GitHub checks have run remotely. After creating the pull request, all four GitHub status checks must also pass before merging.