TASK: Create the Exzibo Manager native Expo application foundation.

Work directly and efficiently. Do not use additional agents, perform a broad audit, generate custom icons, take screenshots or create long documentation.

REPOSITORY SAFETY

Before creating files:

1. Confirm the Git remote points to:
   shriqrdotcom/exzibo-mag-mobile-app-18

   Accept HTTPS or SSH format.

2. Stop if:
   - the remote points to exzibo-mag
   - server.js is present
   - database code is present
   - Vercel API handlers are present
   - an existing mobile application would be overwritten

3. A README, licence and `.gitignore` are allowed.

4. Create or switch to this local branch:

   feature/expo-foundation-auth

   If the repository has an existing initial commit, branch from it.

5. Do not create another Git repository or nested application directory.

6. Do not commit or push anything.

GOAL

Create a real native Android application using:

- Expo SDK 57
- React Native
- TypeScript
- Expo Router
- Better Auth Expo client
- Expo SecureStore

Implement only:

- Expo project foundation
- Google authentication client
- secure session restoration
- authenticated API client
- mobile bootstrap integration
- restaurant selection
- placeholder authenticated home
- logout

Do not implement menu management yet.

1. CREATE THE EXPO PROJECT

Create the project in the current repository root using the official template:

npx create-expo-app@latest --template default@sdk-57 --no-agents-md

If the current root is non-empty and the command cannot initialize there:

1. generate the template in a temporary directory
2. copy only the Expo project files into the repository root
3. preserve the existing `.git`, README and licence
4. do not copy a second `.git` directory
5. remove the temporary directory afterward

Remove unused template demonstration screens and example tab content.

Requirements:

- TypeScript strict mode
- Expo Router
- managed Expo workflow
- React Context and hooks for application state
- no Redux or other large state library
- no android/ or ios/ directories
- do not run expo prebuild

Use these provisional identifiers:

- Display name: Exzibo Manager
- Slug: exzibo-manager
- Scheme: exzibo-manager
- Android package: online.exzibo.manager
- Version: 0.1.0

Use template assets as temporary placeholders. Do not generate branding or custom icons.

If no package manager is established, use npm and keep only `package-lock.json`. Do not create multiple lockfiles.

2. DEPENDENCIES

Verify that these exact packages can install together:

- better-auth@1.6.23
- @better-auth/expo@1.6.23

Stop only if an actual peer-dependency, TypeScript or build incompatibility is confirmed. Do not treat missing SDK 57 wording in documentation as proof of incompatibility.

Install:

- better-auth@1.6.23
- @better-auth/expo@1.6.23

Using `npx expo install`, ensure compatible SDK 57 versions of:

- expo-secure-store
- expo-network
- expo-linking
- expo-web-browser
- expo-constants

Do not install:

- backend or database packages
- Vercel packages
- Cloudflare or R2 packages
- Redis packages
- Bearer-token packages
- web-only UI libraries
- another navigation library
- another validation library

Use small TypeScript runtime type guards instead of adding a validation package.

3. PUBLIC CONFIGURATION

Create `.env.example` containing only:

EXPO_PUBLIC_BACKEND_URL=https://dashboard.exzibo.online

For local validation, create an ignored `.env.local` containing the same public URL if required.

Create a typed configuration module that:

- reads `process.env.EXPO_PUBLIC_BACKEND_URL` using direct dot notation
- removes trailing slashes
- throws a clear configuration error when missing
- never logs the value

Ensure `.gitignore` excludes:

- .env
- .env.local
- .env.*.local
- node_modules
- .expo
- dist
- android
- ios
- signing files
- keystore files
- generated build output

Keep `.env.example` trackable.

Never add:

- BETTER_AUTH_SECRET
- SESSION_SECRET
- DATABASE_URL
- Google client secret
- R2 credentials
- Redis credentials
- Cloudflare credentials
- superadmin configuration

4. BETTER AUTH CLIENT

Create one Better Auth client using:

- `createAuthClient` from `better-auth/react`
- `expoClient` from `@better-auth/expo/client`
- `expo-secure-store`
- scheme `exzibo-manager`
- storage prefix `exzibo-manager`
- the configured backend URL as `baseURL`

Allow the Expo plugin to manage and cache Better Auth cookies and session information in SecureStore.

Requirements:

- use `authClient.useSession()` for session state
- do not manually store a session token
- do not use AsyncStorage for authentication
- do not add Bearer authentication
- do not create custom access or refresh tokens
- do not log cookies, sessions or OAuth responses
- do not create a second manual OAuth implementation

5. AUTHENTICATED API CLIENT

Create one reusable API client using `EXPO_PUBLIC_BACKEND_URL`.

For authenticated requests:

1. obtain cookies with `authClient.getCookie()`
2. add them to the `Cookie` request header
3. use `credentials: "omit"`
4. send and accept JSON
5. handle empty and non-JSON responses safely
6. provide safe network-error messages
7. treat 401 as an invalid session
8. never log headers or cookies

Do not access the database directly.

6. MOBILE BOOTSTRAP

Connect to:

GET /api/mobile/v1/bootstrap

Create TypeScript definitions for:

- BootstrapUser
- RestaurantRole
- RestaurantPermission
- BootstrapRestaurant
- BootstrapResponse

Use runtime type guards to accept only:

- `apiVersion` equal to `v1`
- valid user fields
- a restaurants array
- roles: owner, admin, manager or staff
- string permission values
- permanent string restaurant IDs

Reject malformed responses with a safe recoverable error.

Never construct, infer or guess a restaurant ID. Use only IDs returned by the server.

7. ROUTES AND STATE

Create:

app/
  _layout.tsx
  index.tsx
  (auth)/
    sign-in.tsx
  auth/
    callback.tsx
  (app)/
    select-restaurant.tsx
    home.tsx

Create focused modules under:

src/
  api/
  auth/
  components/
  config/
  providers/
  restaurants/
  storage/
  types/

Support these states:

- session loading
- signed out
- authentication in progress
- bootstrap loading
- no restaurant access
- restaurant selection
- authenticated home
- recoverable network error

Routing behaviour:

- signed-out user → sign-in
- authenticated user → load bootstrap
- one restaurant → select automatically
- multiple restaurants → selection screen
- no restaurants → no-access state with logout
- valid restaurant selection → home
- invalid session → clear restaurant selection and return to sign-in
- network failure → visible retry action

Keep redirect decisions centralized and prevent redirect loops while state is loading.

8. GOOGLE SIGN-IN

Create a native “Continue with Google” button using:

authClient.signIn.social({
  provider: "google",
  callbackURL: "/auth/callback"
})

Requirements:

- use the Better Auth Expo browser/deep-link flow
- let the Expo plugin convert the relative callback path
- prevent repeated button presses
- show progress during authentication
- show only safe user-facing errors
- restore or refetch the session after callback
- load bootstrap after authentication
- do not place Google credentials in the app

Do not claim Google authentication works until it is tested using an Android development build.

9. RESTAURANT SELECTION

Store only the selected permanent restaurant ID in SecureStore using an app-specific storage key.

After every bootstrap response:

- verify that the stored restaurant ID remains in the returned list
- clear it if access was removed
- automatically select the only restaurant
- show a selection screen for multiple restaurants
- provide a switch-restaurant action
- show restaurant name and role

The stored restaurant ID and permissions are UI context only. Backend APIs remain authoritative.

10. HOME AND LOGOUT

Create a minimal native home screen displaying:

- Exzibo Manager
- selected restaurant name
- current role
- switch restaurant action when applicable
- logout action
- “Menu management will be added next”

Logout must:

1. call Better Auth sign-out
2. clear only the selected restaurant ID
3. clear authenticated application state
4. return to sign-in

Use React Native components only. Do not use HTML, CSS, Tailwind, WebView or copied web-dashboard components.

11. VALIDATION

Run:

- `npx expo install --check`
- TypeScript checking
- existing lint command
- `npx expo-doctor`
- `npx expo export --platform android --output-dir dist`

Remove or leave `dist` ignored after validation.

Do not run EAS Build or create signing credentials.

Report Google OAuth, deep linking, cookie restoration and physical-device behaviour as pending unless they were genuinely tested in an Android development build.

DO NOT

- modify the backend repository
- copy the web dashboard
- add backend or database code
- implement menu management
- implement realtime, uploads or notifications
- run expo prebuild
- create android/ or ios/
- create a Git commit
- push to GitHub
- create a pull request
- merge branches
- use Replit Publish
- deploy anything
- expose secrets

FINAL RESPONSE

Return only:

1. verified repository
2. local branch
3. Expo, React Native and TypeScript versions
4. provisional application identifiers
5. dependencies installed
6. important files created
7. authentication flow summary
8. bootstrap and restaurant-selection flow
9. public environment-variable names
10. validation results
11. physical-device tests still required
12. required backend trusted origins:
    - exzibo-manager://
    - exzibo-manager://*
13. confirmation that nothing was committed, pushed or deployed
14. final result: PASS or FAIL